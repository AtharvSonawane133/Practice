{
  "name": "tailwind",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-css": "npx tailwindcss -i ./src/input.css -o ./dist/output.css --watch",
    "dev": "browser-sync start --server dist --files dist/*.html dist/*.css --port 8080 --open"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "live-server": "^1.2.2",
    "tailwindcss": "^3.4.0"
  }
}
